# Tailscale machine hostname
TS_HOSTNAME=mac-mini.siphion.dev

# JWT signing key (generate with: openssl rand -hex 32)
JWT_SHARED_KEY=

# Initial admin credentials (used only on first startup to create the account)
AUTHP_ADMIN_USER=admin
AUTHP_ADMIN_EMAIL=admin@localhost
AUTHP_ADMIN_SECRET=
